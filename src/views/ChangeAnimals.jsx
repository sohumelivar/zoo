const React = require('react');
const Layout = require('./Layout');

module.exports = function ChangeAnimals({ title }) {
  return (
    <Layout title={title}>
      <h3>Change Animals</h3>
    </Layout>
  );
};
